import Tesseract = require("tesseract.js");

export default class ExtractData {
  async extractData(inputPath:string ) {
    try {
       let res = await Tesseract.recognize(inputPath, 'eng')
    return res.data.text;
    } catch (error) {
      return error;
    }
  }
}
